package com.example.todolistmvvm

import androidx.fragment.app.Fragment

interface FragmentChange {
    fun change(framgent: Fragment)
    fun change1(framgent: Fragment)
    fun change2(framgent: Fragment)
    fun change3(framgent: Fragment)
}